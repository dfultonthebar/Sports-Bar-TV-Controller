**HARD BUDGET — read carefully:** Maximum **5 tool calls** total. Each call re-sends the full message history → 429 rate limits → operator waits. Plan tools BEFORE calling. Batch bash one-liners (`&&`-chained), prefer one combined sqlite3 with multiple SELECTs over multiple. Do NOT read every doc file — only target what the diff actually touched. After 5 calls, EMIT your DECISION. CAUTION is fine when budget exhausted; STOP only on a real blocker you saw.

**OUTPUT SHAPE (mandatory):** Your final text response MUST start with one of these three lines on its own line:
```
DECISION: GO
DECISION: CAUTION - <one-line reason>
DECISION: STOP - <one-line reason>
```
Then 3-5 short bullets of justification. Do NOT bury the DECISION inside prose. The auto-update script greps for the DECISION line and STOPs the update if it can't parse one.

**Verify-sql markers MAY be wrong for this location.** A marker that fails because a feature isn't used at THIS location (e.g. Fire TV inputChannel marker fails because location has no Fire TVs) is NOT a real blocker — it's a marker scoped too broadly. In that case emit CAUTION (with reason "verify-sql marker N/A at this location") instead of STOP.

**Per CLAUDE.md §6 + §10, hardware device IPs/ports are stored in the DB tables (`AudioProcessor`, `FireTVDevice`, `DirecTVDevice`, etc.), not in `apps/web/src/lib/hardware-config.ts`.** Empty / generic values in that file are NORMAL on main (each location's DB has the real values). Do NOT STOP just because `hardware-config.ts` shows empty `processorIp` after a merge — it is intentionally a fallback, not the source of truth. Verify via `sqlite3 production.db "SELECT * FROM AudioProcessor"` if you want to confirm the location actually has the device configured.

## COMMON FALSE POSITIVES — these LOOK like blockers but ARE NOT, do not STOP on them:

| Symptom | Why it's NOT a blocker |
|---|---|
| `apps/web/src/lib/hardware-config.ts` has empty `processorIp`, `processorId`, `audioOutputSlots: []` | DB is source of truth (`AudioProcessor` table). TS file is a fallback for fresh installs only. |
| `apps/web/data/directv-devices.json` is 15 bytes (`{"devices":[]}`) | Seed-only file. Real DirecTV devices are in `DirecTVDevice` DB table after first run. CLAUDE.md §6. |
| `apps/web/data/firetv-devices.json` is 15 bytes (`{"devices":[]}`) | Same — seed-only. Real Fire TVs in `FireTVDevice` DB table. |
| `apps/web/data/tv-layout.json` is empty/template (61 bytes) | Layout migrated to `BartenderLayout` DB table in v2.11.0. JSON is seed-only. |
| `apps/web/data/channel-presets-cable.json` / `-directv.json` is `{"presets":[]}` | Seed-only on main. Each location's branch has its own populated copy; auto-update keeps location's via `LOCATION_PATHS_OURS`. |
| `apps/web/data/atlas-configs/*.json` missing | Each location has its own. Gitignored on main. |
| `ecosystem.config.js` has `process.env.SPORTS_GUIDE_API_KEY` (not literal value) | CORRECT — values come from `.env` per location. The hardcoded version was the security leak v2.32.38 fixed. |
| `.env` has `SPORTS_GUIDE_API_KEY` set | This IS the source of truth. Don't STOP unless it's MISSING. |
| New schema tables added to `packages/database/src/schema.ts` and `drizzle-kit push` already ran | Tables will exist. The `schema_push` step runs BEFORE this checkpoint. Don't double-check. |
| `next.config.js` has new redirect rule | Additive; route consolidation is documented in CLAUDE.md §8a. |
| Package version bump in `package.json` is "huge" (e.g. 14 versions) | Locations get bunched updates. Common after a multi-version session. Not a blocker. |

## REAL STOP CONDITIONS — these ARE actual blockers, STOP on these:

- Schema migration that drops a column or table the code still reads (`drizzle-kit push` would have warned but check the merge for breaking changes)
- New required env var introduced in code that is NOT in this location's `.env` (and isn't covered by VERSION_SETUP_GUIDE auto-add step)
- Hardcoded credentials/secrets ending up in shared files (the v2.32.38 issue)
- Working tree has uncommitted changes that the merge would have overwritten (PRE_MERGE_RESET_PATHS in v2.32.26 should have caught these)
- DB query that returned empty when CLAUDE.md says it must have rows (e.g. `Location` table empty → auth broken)

You are doing post-merge review. The git merge from origin/main onto the
location branch completed without conflicts, `npm ci` has finished
installing dependencies, AND `drizzle-kit push` has run to apply any new
schema. Before I run `npm run build`, confirm the merged state is coherent
with this location's configuration.

REQUIRED READS:
0. Read /home/ubuntu/Sports-Bar-TV-Controller/CLAUDE.md IN FULL before
   anything else. This is the project's authoritative rulebook — it may
   have been updated by the merge you're reviewing. Then:
   a. Read ~/.claude/projects/-home-ubuntu-Sports-Bar-TV-Controller/memory/MEMORY.md
      to see what's already saved.
   b. For every section of CLAUDE.md (architecture, conventions, gotchas,
      deployment workflow, hardware config, version bumping, commit
      strategy), check if there's a corresponding memory entry. If not,
      create one. Key items that MUST have memory entries:
      - Version bumping rules (minor vs patch, required on every push)
      - Commit strategy (software to main first, location data stays on branch)
      - Turbo cache gotcha (force rebuild after package changes)
      - Location data merge risk (main has empty templates)
      - drizzle-kit push silent failure mode (pre-existing index aborts)
      - PM2 restart method (delete+start, not restart --update-env)
      - Request body consumption bug (never call request.json() after validateRequestBody)
      - CEC vs IR control (Spectrum = IR only)
      - Current location hardware details (IPs, devices, rooms)
   c. Update any memories that conflict with what CLAUDE.md now says.
   d. Remove memories that reference files/features that no longer exist.
1. The merge commit's diff: run `git -C /home/ubuntu/Sports-Bar-TV-Controller show HEAD`
2. Any .md docs in /home/ubuntu/Sports-Bar-TV-Controller/docs/ that were
   modified by the merge — read each in full. LOCATION_UPDATE_NOTES.md
   especially: a v2.8.0+ entry will tell you whether this release added
   new tables and what their names are.
2a. `docs/VERSION_SETUP_GUIDE.md` — for EVERY version between the
   pre-merge and post-merge version numbers, find its entry and check
   the "Required manual steps" list. For each step, either:
     (a) verify it was already done at this location (run the
         verification command from the entry), OR
     (b) perform the step automatically if it's safe (e.g., ALTER TABLE
         ADD COLUMN, INSERT a missing row), OR
     (c) flag it in your DECISION response as a post-update task the
         operator must run.
   If the target version has no entry in VERSION_SETUP_GUIDE.md, note
   that as a DRIFT issue — whoever bumped the version skipped documenting
   setup requirements. Mention it after the DECISION line so the operator
   can backfill the doc.

2b. **Verify-SQL markers (v2.32.7+)** — within each version entry from
   step 2a, look for HTML comments shaped like
   `<!-- verify-sql: SELECT ... -->` (one or more). Each is a HARD
   precondition for the version to be considered correctly applied at
   this location.

   To extract them safely (the SQL may contain `>` which would trip a
   naive `[^>]+` regex), use grep -oP non-greedy:
     `grep -oP '<!--\s*verify-sql:.*?-->' docs/VERSION_SETUP_GUIDE.md`

   For each marker found:
     i.   Strip the wrapping comment to get just the SQL.
     ii.  Run the SQL: `sqlite3 /home/ubuntu/sports-bar-data/production.db
          "<the SQL>"` and capture the row count returned.
     iii. If the SELECT returns 0 rows: that precondition is NOT met.
          You MUST return `DECISION: STOP - <version> verify-sql failed:
          <the SQL>` with a follow-up line explaining what the operator
          needs to do to satisfy it (usually visible in the same entry's
          "Required manual steps" section just above the marker).
     iv.  If it returns ≥1 row: the precondition is met. Continue to
          the next marker.

   Each marker may also have an adjacent `<!-- verify-description: ... -->`
   comment with human-readable context — include it in your STOP message
   if present. Markers are forward-only enforcement: entries that
   pre-date v2.32.7 don't have markers and remain advisory (covered by
   step 2a's flag-it-in-the-DECISION behavior).
3. Current state of /home/ubuntu/Sports-Bar-TV-Controller/apps/web/data/*.json
   — these should NOT have changed during merge.
4. The .env file at /home/ubuntu/Sports-Bar-TV-Controller/.env — confirm
   expected keys exist (SPORTS_GUIDE_USER_ID, and any new keys the diff
   introduced). Do NOT log secret values, only confirm key presence.
5. The ChannelPreset table counts:
   `sqlite3 /home/ubuntu/sports-bar-data/production.db "SELECT COUNT(*), deviceType FROM ChannelPreset GROUP BY deviceType"`
6. The current run log — grep for lines starting with "WARNING:" or
   tagged with "drizzle". A clean schema push produces no warnings. A
   "pre-existing objects (benign)" warning means drizzle-kit hit an
   already-existing index/table and continued — this is safe and expected
   at locations that previously had manual hotfix tables. Any OTHER
   schema-push error would have aborted auto-update.sh before reaching
   you, so any [SCHEMA] message in the log is the benign branch.
7. If the merge diff modified `packages/database/src/schema.ts`, verify
   the new tables actually exist in production.db:
   `sqlite3 /home/ubuntu/sports-bar-data/production.db ".tables"` and
   compare against the export names in the schema diff. Missing tables
   are the most common silent failure mode of this whole pipeline — they
   make the build succeed but new endpoints 500 at runtime.

DECISION CRITERIA:
- GO: merged state is coherent, config files intact, no unexpected changes
  to location-specific paths, ChannelPreset table is populated, AND any
  new schema tables from the diff exist in production.db.
- CAUTION: merged state is coherent but has warnings (e.g., a new .env
  variable would be helpful but the app can still start in degraded mode,
  or schema push warned about pre-existing objects but the new tables ARE
  present in the .tables output).
- STOP: a location-specific config file was modified by the merge (indicates
  conflict resolution failed), OR a required new .env variable is missing,
  OR the ChannelPreset table has become empty, OR a new table that the
  schema diff added is MISSING from production.db (indicates the schema
  push step failed silently — needs manual `sqlite3` CREATE before the
  build is worth running).

OUTPUT FORMAT — CRITICAL:
Your response MUST contain a line starting with exactly "DECISION: "
followed by GO, CAUTION, or STOP. The auto-update script parses this
with grep. If the line is missing, the update is treated as FAILED and
triggers a full rollback — even if your analysis shows everything is fine.

Do your memory sync and analysis work first, then output:
DECISION: GO|CAUTION|STOP - <reason>

Put the DECISION line BEFORE any summary text, or on its own line.
Do NOT bury it inside a paragraph or markdown block.

STOP at Checkpoint B aborts the build and triggers a full rollback
(git reset --hard to the pre-update tag, npm ci, restore .next.bak).

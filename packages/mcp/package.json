{
  "name": "@sports-bar/mcp",
  "version": "1.0.0",
  "description": "Sports-Bar MCP server — exposes read-only system observe tools to Hermes Agent (and any MCP client) by wrapping the existing audited HTTP APIs",
  "private": true,
  "main": "./src/server.ts",
  "scripts": {
    "build": "echo 'MCP server runs via tsx (no build step)'",
    "start": "tsx src/server.ts",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "tsx": "^4.0.0",
    "typescript": "^6.0.0"
  }
}

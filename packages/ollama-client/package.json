{
  "name": "@sports-bar/ollama-client",
  "version": "1.0.0",
  "description": "Remote-first Ollama client with automatic local fallback (central GPU offload)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "private": true,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "node --test --import tsx ./src/__tests__/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "typescript": "^6.0.0"
  }
}

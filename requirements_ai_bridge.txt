
# AI Bridge Dependencies
# ======================
# Additional requirements for the AI-to-AI communication system

# Core dependencies
aiohttp>=3.8.0
asyncio-throttle>=1.0.2
pyyaml>=6.0
dataclasses-json>=0.5.7

# AI Provider SDKs
openai>=1.0.0
anthropic>=0.7.0
# Note: Grok SDK not yet available, using direct HTTP calls

# Async and concurrency
asyncio>=3.4.3
aiofiles>=23.0.0

# Metrics and monitoring
prometheus-client>=0.16.0
psutil>=5.9.0

# Configuration and utilities
python-dotenv>=1.0.0
jsonschema>=4.17.0

# Testing (development)
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0

# Optional: Enhanced logging
structlog>=22.0.0
colorlog>=6.7.0

# Optional: Database for persistent storage
# sqlite3 (built-in)
# or for production:
# asyncpg>=0.28.0  # PostgreSQL
# aiomysql>=0.1.1  # MySQL
